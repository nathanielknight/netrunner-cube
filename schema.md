For deckbuilding I want:

- `attributes.stripped_title` (easier to type)
- `attributes.side_id`
- `attributes.faction_id`
- `attributes.influence_cost`
- `attributes.card_type_id`
- `attributes.card_subtype_ids`
- `attributes.cost`
- `attributes.trash_cost`


For agendas:

- `attributes.advancement_requirement` (to show this for agendas)
- `attributes.agenda_points`

For programs/ice

- `attributes.memory_cost`
- `attributes.strength`


I also want to be able to know if I have a copy.

